# Plan: Deploy students Edge Function

## Current Status
The fix for session_mode and session_time is already in the local file (lines 76-77):
```js
session_mode: s.session_mode || null,
session_time: s.session_time || null,
```

## What User Needs
User needs the full `students/index.ts` file to deploy to Supabase Edge Functions. The file already contains the fix.

## Action
Copy the full file content below and deploy to Supabase Dashboard → Edge Functions → students → Deploy:

(Content in D:\MY\chesskidoo-ai-admin\supabase\functions\students\index.ts - 257 lines - already has session_mode/session_time fix at lines 76-77)

After deploying, refresh the website to verify session counts update correctly.